apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/pre
images:
  - name: server
    newName: server
    newTag: v0.0.0
patches:
  - |-
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: server
    spec:
      template:
        spec:
          containers:
            - name: server
              envFrom:
                - prefix: db_
                  secretRef:
                    name: pgo-cluster-pguser-example-staging
